{
  "$schema": "http://json-schema.org/schema",
  "id": "MatDayjsDateFormats",
  "title": "MatDayjsAdapter Date Formats",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the service.",
      "visible": false
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
   }
}
